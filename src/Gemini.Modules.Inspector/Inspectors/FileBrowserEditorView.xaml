<UserControl x:Class="Gemini.Modules.Inspector.Inspectors.FileBrowserEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:p="clr-namespace:Gemini.Modules.Inspector.Properties"
             xmlns:l="http://gu.se/Localization"
             mc:Ignorable="d" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Content="{l:Static p:Resources.FileBrowserSelect}" cal:Message.Attach="Select" Padding="5,0" />
        <Label Grid.Column="1" Content="{Binding Value}" Margin="3,0,0,0" />
    </Grid>
</UserControl>
